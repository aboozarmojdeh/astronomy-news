
<header class="header">
  <h1 id="neon">ASTRONOMY NEWS SCRAPER</h1>
  <nav class="button-group">
    <button class="button-dark" id="scrape" data-toggle="modal" data-target="#alertModal">Scrape New Articles</button>
    <a href="/articles/viewSaved"><button class="button-dark" id="viewSaved">View Saved Articles</button></a>
    <a href="http://www.astronomy.com/news" target="_blank"><button class="button-dark" id="viewSaved">View Ref. Website</button></a>

  </nav>
</header>
<div class="filler"></div>

{{#each articles}}
  {{#unless saved}}
    <figure class="storyPanel clearfix">
      <a href="{{link}}" class="pull-right" target="_blank">
        <div class="imgBox">
          <img src="{{img}}" alt="" class="storyImg" height="150">
        </div>
      </a>
      {{#if link}}
        <a href="{{link}}" class="storyLink" target="_blank">
          <h2 class="storyHeadline">{{title}}</h2>
        </a>
      {{else}}
        <h3 class="storyHeadline">{{title}}</h3>
      {{/if}}
      {{!-- <h5 class="storyByline">{{byLine}}</h5>  --}}
      <h6 class="storySummary">{{description}}</h6>
      <button class="storyButton" id="saveArticle" data-id="{{_id}}">Save Article</button>
    </figure>
  {{/unless}}
{{/each}}

{{!-- Modal for alerting number of scraped articles --}}
<div id="alertModal" class="modal fade" role="dialog">
  <div class="modal-dialog modal-md">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
      </div>
      <div class="modal-body text-center">
        <h2>You have scraped <span id="numArticles"></span> new articles</h2>
      </div>
    </div>
  </div>
</div>

{{!-- error modal --}}
<div id="error" class="modal fade" role="dialog">
  <div class="modal-dialog modal-md">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
      </div>
      <div class="modal-body">
        <h2>There was an error processing your request.  Please try again later.</h2>
      </div>
    </div>
  </div>
</div>
